/* Defines the product entity */
export interface Product {
  id: number;
  productName: string;
  productCode: string;
  category: string;
  releaseDate: string;
  price: number;
  description: string;
}
