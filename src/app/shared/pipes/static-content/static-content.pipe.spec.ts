import { StaticContentPipe } from './static-content.pipe';

describe('StaticContentPipe', () => {
  it('create an instance', () => {
    const pipe = new StaticContentPipe();
    expect(pipe).toBeTruthy();
  });
});
