import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class LoggerService {
  /**
   * // TODO: Post errors to service
   * Logs error
   * @param message error message
   * @param stack error stack
   */
  logError(message: string, stack: string): void {
    // TODO: Post errors to service
  }
}
